{
  "name": "workon-engine",
  "version": "0.1.0",
  "description": "Reference gate plugin — demonstrates the hooks.gate CLI-interception contract (block/replace/passthrough). Engine resolution for workon is owned natively; this is an example only.",
  "sdk": "*",
  "entry": { "kind": "wasm", "path": "plugin.wasm", "export": "handle" },
  "capabilities": [
    "sdk:config:read",
    "sdk:config:write"
  ],
  "hooks": {
    "gate": ["cmd:workon"]
  }
}
